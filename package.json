{
  "name": "automation-platform-mvp",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.0.9"
  }
}
